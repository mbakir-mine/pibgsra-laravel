<x-app-layout>
    <x-slot name="header"><h2 class="text-2xl font-semibold text-slate-900">Tetapan Yuran Sekolah</h2></x-slot>
    <div class="py-8"><div class="mx-auto max-w-3xl px-4 sm:px-6 lg:px-8"><div class="rounded-lg border border-slate-200 bg-white p-6 shadow-sm">
        <div class="mb-6 border-b border-slate-100 pb-5"><div class="text-sm font-semibold uppercase tracking-wide text-teal-700">{{ $category }}</div><h3 class="mt-1 text-xl font-semibold text-slate-900">{{ $school->name }}</h3><p class="mt-2 text-sm text-slate-500">Tetapan ini digunakan untuk semua murid dan keluarga di sekolah ini.</p></div>
        <form method="POST" action="{{ route('fees.store') }}" class="space-y-4">@csrf<input type="hidden" name="school_id" value="{{ $school->id }}"><div class="overflow-hidden rounded-lg border border-slate-200"><div class="grid grid-cols-[1fr_9rem] gap-4 bg-slate-50 px-4 py-3 text-sm font-semibold text-slate-700"><span>Item yuran</span><span>Amaun (RM)</span></div>@foreach($items as $index => $item)<div class="grid grid-cols-[1fr_9rem] items-center gap-4 border-t border-slate-100 px-4 py-3"><div><input type="hidden" name="items[{{ $index }}][code]" value="{{ $item[0] }}"><input type="hidden" name="items[{{ $index }}][name]" value="{{ $item[1] }}"><span class="text-sm font-medium text-slate-800">{{ $item[1] }}</span></div><input name="items[{{ $index }}][amount]" type="number" min="0" step="0.01" value="{{ $item[2] }}" class="w-full rounded-md border-slate-300 text-right" required></div>@endforeach</div><div class="flex items-center justify-between rounded-lg bg-teal-50 px-4 py-3"><span class="font-semibold text-teal-900">Jumlah pakej</span><span class="text-xl font-bold text-teal-900">RM {{ number_format(collect($items)->sum(fn($item) => $item[2]), 2) }}</span></div><div class="flex justify-end"><button class="rounded-md bg-teal-700 px-5 py-2.5 font-semibold text-white hover:bg-teal-800">Simpan tetapan sekolah</button></div></form>
    </div></div></div>
</x-app-layout>
