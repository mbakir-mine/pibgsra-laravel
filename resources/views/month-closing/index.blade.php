<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl text-gray-800 leading-tight">Penutupan Bulan</h2></x-slot><div class="py-8"><div class="max-w-7xl mx-auto sm:px-6 lg:px-8"><div class="bg-white p-6 shadow-sm sm:rounded-lg"><form method="POST" action="{{ route('month-closing.store') }}" class="mb-8 flex flex-wrap gap-3">@csrf<select name="school_id" class="rounded border">@foreach($schools as $school)<option value="{{ $school->id }}">{{ $school->name }}</option>@endforeach</select><input name="year" type="number" value="{{ now()->year }}" class="w-28 rounded border"><input name="month" type="number" min="1" max="12" value="{{ now()->month }}" class="w-20 rounded border"><button class="rounded bg-indigo-600 px-4 py-2 text-white">Tutup bulan</button></form><table class="w-full text-left"><thead><tr><th class="p-2">Sekolah</th><th class="p-2">Bulan</th><th class="p-2">Status</th><th class="p-2">Ditutup pada</th></tr></thead><tbody>@forelse($closings as $closing)<tr class="border-t"><td class="p-2">{{ $closing->school?->name }}</td><td class="p-2">{{ $closing->month }}/{{ $closing->year }}</td><td class="p-2">{{ $closing->status }}</td><td class="p-2">{{ $closing->closed_at?->format('d/m/Y H:i') }}</td></tr>@empty<tr><td colspan="4" class="p-4 text-slate-500">Tiada penutupan bulan.</td></tr>@endforelse</tbody></table><div class="mt-4">{{ $closings->links() }}</div></div></div></div></x-app-layout>
